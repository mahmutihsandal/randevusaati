# Project Instructions

- Work in this repository for the website: `/Users/mahmutdal/StudioProjects/hair_design/docs-web`.
- `docs-web` is an independent GitHub repository. For website changes, run Git commands from this directory and commit/push to its `origin`; do not use the parent Flutter repository's Git history.
- Do not take screenshots or run visual screenshot checks by default; they take too long in this workflow.
- Implement requested changes directly. If a visual issue remains, the user will provide a screenshot.
- Keep `assets/screens/admin/.DS_Store` out of commits unless explicitly requested.
